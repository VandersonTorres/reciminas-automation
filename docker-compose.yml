version: "3.9"

services:
  web:
    build: .
    container_name: django_app
    ports:
      - "8000:8000"
    env_file:
      - server/.env
    volumes:
      - ./downloads:/app/server/downloads
    restart: always
